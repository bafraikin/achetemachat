<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @item.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @item.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= @item.price %>
</p>

<p>
  <strong>Image url:</strong>
  <%= @item.image_url %>
</p>

<% if @item.image.attached? %>
  <p>
    <strong>Image:</strong>
    <br>
    <%= image_tag @item.image %>
  </p>
<% end %>
<img src=<%Rails.application.routes.url_helpers.rails_blob_path(@item.image, only_path: true)%> > </img>
<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
